---
name: project-structure
description: 项目级数据管理 SOP。Use when 需要规范目录结构、命名、索引切片、生命周期与产出物管理。
---

# 项目级数据管理 SOP

> ⚠️ 根据下方路由表跳到需要的 section, 不要全部阅读。
> 全链路中每个环节产生的文件, 在哪创建 / 怎么命名 / 写什么 / 何时删除, 全部在此规范。

## 路由表

| 关键词 | 跳转 |
|--------|------|
| `目录`, `结构`, `新项目`, `初始化` | → §1 目录结构 + §7 初始化清单 |
| `分类`, `生命周期`, `持久`, `临时` | → §2 文件分类总览 |
| `spec`, `plan`, `tracker`, `audit`, `checkpoint` | → §3 对应子节 (§3.3-§3.7) |
| `命名`, `task-name`, `task-id`, `前缀` | → §4 命名规范 |
| `索引`, `切片`, `拆分`, `过大` | → §5 索引与切片策略 |
| `创建`, `写入`, `删除`, `清理`, `健康检查` | → §6 写入/删除逻辑速查表 |
| `引用`, `依赖`, `谁用` | → §8 引用关系图 |

---

## 1. 标准目录结构

```
.agent/.agent/projects/{project}/
│
├── CONTEXT.md                         ← 项目入口 (技术栈/铁律/阶段指针)
├── roadmap.md                         ← 路线图 (Phase 列表/当前指针)
│
├── playbooks/                         ← 项目级实施方案 (how-to)
│   └── {domain}.md
│
├── reference/                         ← 技术参考资料 (持久)
│   └── {topic}.md
│
└── data/                              ← 运行时数据 (按生命周期管理)
    ├── specs/                         ← 需求文档
    ├── plans/                         ← 任务分配单
    ├── progress/                      ← 进度跟踪 + 统计
    │   ├── ACCEPTED.md                ← 已验收清单 (持久)
    │   ├── requirements-list.md       ← 需求总览 (持久)
    │   ├── risk-register.md           ← 风险登记 (持久)
    │   ├── engineering-status.md      ← 工程状态 (持久)
    │   ├── user-feedback.md           ← 用户反馈 (持久)
    │   ├── PROJECT-MEMORY.md          ← 项目复用记忆 (持久)
    │   └── TRACKER-{task-id}.md       ← 任务追踪器 (临时)
    ├── checkpoints/                   ← 断点存档 (临时)
    ├── audits/                        ← 审计报告 (修复后删)
    ├── errors/                        ← 错题本 (持久)
    │   └── ERROR-BOOK.md
    └── training/                      ← 经验教训 (持久)
```

---

## 2. 文件分类  总览

### 2.1 文件生命周期分类

| 类型 | 含义 | 目录 | 删除时机 |
|------|------|------|---------|
| **持久** 🟢 | 项目存续期间始终保留 | 根级 + reference + playbooks + errors + training | 项目归档时才清理 |
| **累积持久** 🟢 | 只追加不删除, 越积越多 | progress/(ACCEPTED, req-list, risk, feedback) | 永不删 |
| **任务级** 🟡 | 跟随某个任务, 任务 CLOSED 则删 | specs, plans, TRACKER, checkpoints | 任务 CLOSED |
| **修复后删** 🟠 | 审计报告, 问题修复即删（默认） | audits | 修复完成立即删除 |
| **重构基线保留** 🔵 | V1→V3/架构/traceability 基线审计 | audits | 长期保留（用于未来重构对照） |

### 2.2 全链路产出物映射

```
用户说需求
  ↓
PM 领悟: 无产出物
  ↓
PM GATHER + SPEC:
  创建 → data/specs/{spec}.md              [任务级]
  创建 → data/progress/TRACKER-{id}.md     [任务级]
  更新 → data/progress/requirements-list.md [累积持久]
  ↓
用户确认:
  更新 → specs/{spec}.md §9 标记 CONFIRMED
  ↓
CTO 分配:
  创建 → data/plans/{plan}.md              [任务级]
  更新 → data/progress/engineering-status.md [累积持久]
  ↓
工程师执行:
  更新 → data/progress/TRACKER-{id}.md     [任务级]
  ↓
CTO 审查:
  Block → 无新文件, 但更新 TRACKER
  Approve/Warning → 无新文件
  ↓
QA 审计:
  创建 → data/audits/{audit}.md            [修复后删]
  Block → 创建错题本条目 → data/errors/ERROR-BOOK.md [持久]
  ↓
PM 交付:
  更新 → data/progress/user-feedback.md    [累积持久]
  ↓
用户确认 → CLOSED:
  删除 → specs/{spec}.md
  删除 → plans/{plan}.md
  删除 → TRACKER-{id}.md
  删除 → checkpoints/{相关}.md
  更新 → data/progress/ACCEPTED.md (追加)
  审计报告: 如果问题全修复 → 删除
```

---

## 3. 各目录详细规范

### 3.1 CONTEXT.md (项目入口)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/CONTEXT.md` |
| **创建者** | PM (新项目初始化时) |
| **命名** | 固定名, 不可改 |
| **生命周期** | 🟢 持久 — 随项目演进更新 |
| **必须包含** | §1 项目简介, §2 当前阶段指针, §3 技术栈, §4 铁律 |
| **更新时机** | Phase 完成后更新阶段指针; 技术栈变更后更新 |
| **引用者** | requirements.md §1.0 (技术栈确认), contact.md (项目识别) |

### 3.2 roadmap.md (路线图)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/roadmap.md` |
| **创建者** | PM + CTO |
| **命名** | 固定名, 不可改 |
| **生命周期** | 🟢 持久 |
| **必须包含** | Phase 列表 + 完成状态 + 当前指针 |
| **更新时机** | 每个 Phase 完成后, 移动指针到下一个 Phase |

### 3.3 data/specs/ (需求文档)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/specs/` |
| **创建者** | PM (requirements.md Phase 2 SPEC 输出) |
| **命名** | `YYYY-MM-DD_{task-name}.md` |
| **命名示例** | `2026-02-15_vma-employee-phone-field.md` |
| **生命周期** | 🟡 任务级 — CLOSED 后删除 |
| **模板** | requirements.md §2 定义 (含 §1-§9) |
| **写入逻辑** | PM 在 GATHER 后自动生成, 状态写在 §9 |
| **状态流转** | `DRAFT → CONFIRMED → IN_PROGRESS → CLOSED` (写在 §9) |
| **删除逻辑** | 任务 CLOSED → memory.md §2 验收后协议 → 删除此文件 |
| **🔴 铁律** | **目录中不应有 CLOSED 状态的 Spec。有 = 清理遗漏。** |

### 3.4 data/plans/ (任务分配单)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/plans/` |
| **创建者** | CTO (build.md §2 分配时) |
| **命名** | `YYYY-MM-DD_{task-name}_plan.md` |
| **命名示例** | `2026-02-15_vma-employee-phone-field_plan.md` |
| **生命周期** | 🟡 任务级 — CLOSED 后删除 |
| **模板** | build.md §2 CTO→工程师工单格式 + memory.md §4 规划格式 |
| **写入逻辑** | CTO 审查 Spec 后, 按 Phased Delivery 分解并写入 |
| **删除逻辑** | 任务 CLOSED → 删除 |
| **🔴 铁律** | **目录中不应有已完成任务的 Plan。有 = 清理遗漏。** |

### 3.5 data/progress/ (进度跟踪)

此目录包含两类文件: **临时** (Tracker) 和 **累积持久** (其余):

#### 3.5.1 TRACKER-{task-id}.md (临时)

| 项目 | 规范 |
|------|------|
| **命名** | `TRACKER-{task-id}.md` |
| **命名示例** | `TRACKER-VMA-PHONE-001.md` |
| **task-id 规则** | `{MODULE}-{FEATURE}-{NNN}`, 全大写, 连字符分隔 |
| **创建者** | PM (Spec 创建时同步创建) |
| **生命周期** | 🟡 任务级 |
| **写入逻辑** | 每次状态变更、进度更新都追加; 最新在顶部 |
| **删除逻辑** | 任务 CLOSED → memory.md §2 验收后协议 → 删除 |
| **模板** | memory.md §1.2 追踪器格式 |

#### 3.5.2 累积持久文件

| 文件 | 命名 | 创建者 | 写入逻辑 | 生命周期 |
|------|------|--------|---------|---------|
| `ACCEPTED.md` | 固定名 | PM | 每次验收后追加一行 (memory.md §2.3) | 🟢 永不删 |
| `requirements-list.md` | 固定名 | PM | 新需求追加, 完成的标记 ✅ (PM §6.1) | 🟢 永不删 |
| `risk-register.md` | 固定名 | PM | 识别到风险时追加 (PM §6.3) | 🟢 永不删 |
| `engineering-status.md` | 固定名 | CTO | 任务分配/完成时更新 (CTO §6) | 🟢 永不删 |
| `user-feedback.md` | 固定名 | PM | 每次用户反馈后追加 (PM §5.5) | 🟢 永不删 |
| `PROJECT-MEMORY.md` | 固定名 | PM/CTO | 任务结束时沉淀可复用需求（UIUX/数据口径/长期规则） | 🟢 永不删 |

### 3.6 data/checkpoints/ (断点存档)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/checkpoints/` |
| **创建者** | 当前执行角色 (会话即将结束时, handoff.md) |
| **命名** | `YYYY-MM-DD_{task-name}_checkpoint.md` |
| **命名示例** | `2026-02-15_vma-employee-phone-field_checkpoint.md` |
| **生命周期** | 🟡 任务级 — 恢复后或 CLOSED 后删除 |
| **写入逻辑** | 跨会话中断时创建, 含最小恢复信息 |
| **删除逻辑** | 新会话恢复后可删; 任务 CLOSED 时强制删 |
| **模板** | handoff.md §2 Checkpoint 格式 |

### 3.7 data/audits/ (审计报告)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/audits/` |
| **创建者** | QA (qa-auditor.md §2.3 审计报告) |
| **命名** | `YYYY-MM-DD_{task-name}_audit.md` |
| **命名示例** | `2026-02-15_vma-employee-phone-field_audit.md` |
| **生命周期** | 🟠 修复后删（默认临时，不得积压） |
| **写入逻辑** | QA 完成审计后创建, 含 Severity Summary + Verdict |
| **删除逻辑** | 问题修复完成后立即**删除**（默认）；`v1/v2/v3` 基线审计与 traceability 文件不删 |
| **🔴 铁律** | **审计报告不是档案。修复完成必须删除，不得长期占用。** |
| **检查方法** | 报告中有未关闭的 ❌ 项 → 保留; 全 ✅ → 删除 |

### 3.8 data/errors/ (错题本)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/errors/` |
| **文件** | `.agent/projects/{project}/data/errors/ERROR-BOOK.md` (主索引文件, 可按模块切片) |
| **创建者** | QA/CTO/工程师 (发现错误时) |
| **命名** | 固定名, 不可改 |
| **生命周期** | 🟢 持久 — 只追加不删除 |
| **写入逻辑** | 追加新条目 (memory.md §3.2 格式), 更新关键词索引 |
| **模板** | memory.md §3.2 错题本格式 |
| **索引** | 文件顶部维护关键词索引表 (memory.md §3.2) |
| **切片规则** | 如果条目 > 50 条 → 按模块拆分: `ERROR-BOOK-{module}.md` |

### 3.9 data/training/ (经验教训)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/data/training/` |
| **创建者** | QA (qa-auditor.md §3.3 培训输出) |
| **命名** | `YYYY-MM-DD_{topic}.md` |
| **命名示例** | `2026-02-15_prisma-migration-gotchas.md` |
| **生命周期** | 🟢 持久 — 只追加不删除 |
| **写入逻辑** | QA 在审计中发现通用模式时创建 |

### 3.10 playbooks/ (实施方案)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/playbooks/` |
| **创建者** | PM + CTO |
| **命名** | `{domain}.md` (小写, 域名) |
| **命名示例** | `vma.md`, `auth.md`, `products.md` |
| **生命周期** | 🟢 持久 — 只更新不删除 |
| **写入逻辑** | 规划新域时创建, 实施中发现新规则时追加 |

### 3.11 reference/ (参考资料)

| 项目 | 规范 |
|------|------|
| **位置** | `.agent/projects/{project}/reference/` |
| **创建者** | 任何角色 |
| **命名** | `{topic}.md` (小写, 主题名) |
| **命名示例** | `api-field-mapping.md`, `legacy-mysql-schema.md` |
| **生命周期** | 🟢 持久 — 只更新不删除 |
| **切片规则** | 如果单文件 > 500 行 → 按子主题拆分 |

---

## 4. 命名规范总览

### 4.1 文件前缀

| 前缀 | 用法 | 示例 |
|------|------|------|
| `YYYY-MM-DD_` | 所有任务级文件 (spec/plan/checkpoint/audit/training) | `2026-02-15_vma-phone.md` |
| `TRACKER-` | 追踪器 | `TRACKER-VMA-PHONE-001.md` |
| `ERROR-BOOK` | 错题本 | `.agent/projects/{project}/data/errors/ERROR-BOOK.md` |
| 无前缀 | 持久级文件 (ACCEPTED, roadmap, playbook) | `ACCEPTED.md` |

### 4.2 task-name 规则

```
格式: {module}-{feature}
大小写: 全小写, 连字符分隔
长度: ≤ 5 个单词
示例:
  vma-employee-phone-field
  auth-security-code-reset
  products-barcode-export
```

### 4.3 task-id 规则

```
格式: {MODULE}-{FEATURE}-{NNN}
大小写: 全大写
NNN: 3 位顺序号, 从 001 开始
示例:
  VMA-PHONE-001
  AUTH-RESET-002
  PROD-EXPORT-001
```

---

## 5. 索引与切片策略

### 5.1 索引规则

| 目录 | 索引方式 | 说明 |
|------|---------|------|
| `specs/` | 无需索引 | 任务完成即删, 目录应始终为空或只有活跃任务 |
| `plans/` | 无需索引 | 同上 |
| `progress/` | `requirements-list.md` 兼任索引 | 所有需求的状态一览 |
| `audits/` | 无需索引 | 修复后即删, 目录应始终为空或只有活跃审计 |
| `errors/` | `.agent/projects/{project}/data/errors/ERROR-BOOK.md` 顶部关键词索引表 | memory.md §3.2 定义 |
| `training/` | 无需索引 | 按日期排列, 数量少 |
| `reference/` | 如 > 10 个文件 → 创建 `_INDEX.md` | 列出所有文件 + 一句话说明 |
| `playbooks/` | 如 > 5 个文件 → 创建 `_INDEX.md` | 列出所有文件 + 域映射 |

### 5.2 切片规则 (文件过大时)

| 文件类型 | 切片阈值 | 切片方式 |
|---------|---------|---------|
| `.agent/projects/{project}/data/errors/ERROR-BOOK.md` | > 50 条 | 按模块拆: `ERROR-BOOK-{module}.md` + 根文件保留索引 |
| `reference/{topic}.md` | > 500 行 | 按子主题拆: `{topic}-{subtopic}.md` |
| `ACCEPTED.md` | > 200 条 | 按年份归档: `ACCEPTED-{YYYY}.md`, 当前年份在 `ACCEPTED.md` |
| `PROJECT-MEMORY.md` | > 120 条 | 按主题切片: `PROJECT-MEMORY-uiux.md` / `PROJECT-MEMORY-data.md` / `PROJECT-MEMORY-rules.md`，根文件保留索引 |
| `requirements-list.md` | > 100 条 | 已完成的移到 `requirements-archive-{YYYY}.md` |
| `user-feedback.md` | > 100 条 | 按年份归档 |

---

## 6. 写入/删除逻辑速查表

> 统一治理标准：`core/reference/artifact-governance-standard.md`

### 6.1 写入 (CREATE / APPEND)

| 时机 | 创建什么 | 位置 | 模板来源 |
|------|---------|------|---------|
| PM 完成 SPEC | Spec 文件 | `data/specs/` | requirements.md §2 |
| PM 创建 Spec 时 | Tracker 文件 | `data/progress/` | memory.md §1.2 |
| PM 完成 SPEC | 追加 requirements-list | `data/progress/` | PM §6.1 |
| CTO 分配任务 | Plan 文件 | `data/plans/` | build.md §2 |
| 跨会话中断 | Checkpoint | `data/checkpoints/` | handoff.md §2 |
| QA 完成审计 | 审计报告 | `data/audits/` | qa-auditor.md §2.3 |
| 错误被发现 | 追加 ERROR-BOOK | `data/errors/` | memory.md §3.2 |
| QA 提炼经验 | Training 文件 | `data/training/` | qa-auditor.md §3.3 |
| 用户验收 | 追加 ACCEPTED.md | `data/progress/` | memory.md §2.3 |
| 用户反馈 | 追加 user-feedback | `data/progress/` | PM §5.5 |
| 风险识别 | 追加 risk-register | `data/progress/` | PM §6.3 |

### 6.2 删除 (DELETE)

| 时机 | 删什么 | 触发者 | 参考 SOP |
|------|--------|--------|---------|
| 任务 CLOSED | `data/specs/{spec}.md` | PM | memory.md §2 |
| 任务 CLOSED | `data/plans/{plan}.md` | PM | memory.md §2 |
| 任务 CLOSED | `data/progress/TRACKER-{id}.md` | PM | memory.md §2 |
| 任务 CLOSED | `data/checkpoints/{相关}.md` | PM | memory.md §2 |
| 审计问题全修复 | `data/audits/{audit}.md` | QA/CTO | 本 SOP §3.7 |
| 会话恢复后 | `data/checkpoints/{used}.md` | 恢复者 | handoff.md |

### 6.3 健康检查 (定期或任务开始时)

```
Agent 在每个任务开始前, 快速扫描:

[ ] data/specs/ 中有无 CLOSED 状态的 Spec → 如有, 立即删除
[ ] data/plans/ 中有无已完成任务的 Plan → 如有, 立即删除
[ ] data/audits/ 中有无全 ✅ 的审计报告 → 如有, 立即删除
[ ] data/checkpoints/ 中有无过期检查点 → 如有, 立即删除
[ ] data/progress/ 中有无孤立 TRACKER → 如有, 确认后删除
```

---

## 7. 新项目初始化清单

```
PM 创建新项目时:

[ ] 确定项目简称 (project-id), 全小写, ≤ 10 字符
[ ] mkdir -p .agent/projects/{project}
[ ] 创建 CONTEXT.md (§1 简介 + §2 阶段指针 + §3 技术栈 + §4 铁律)
[ ] 创建 roadmap.md (Phase 列表 + 当前指针→Phase 1)
[ ] mkdir -p data/{specs,plans,progress,checkpoints,audits,errors,training}
[ ] mkdir -p playbooks reference
[ ] 创建 data/progress/ACCEPTED.md (空模板, memory.md §2.3 格式)
[ ] 创建 data/progress/requirements-list.md (空表头)
[ ] 创建 data/errors/ERROR-BOOK.md (空模板, memory.md §3.2 格式)
[ ] 注册到 core/SKILL.md L4 客户项目表
[ ] 通知 CTO: "项目 {project} 已就绪"
```

---

## 8. 引用关系图

```
本 SOP 被以下 SOP 引用/依赖:

contact.md        → 识别项目 → 读 CONTEXT.md
requirements.md   → §1.0 读 CONTEXT.md; §2 创建 Spec
build.md          → §2 创建 Plan; §6 CLOSED 时删除
memory.md         → §1 Tracker 格式; §2 验收后删除; §3 Error Book
handoff.md        → Checkpoint 格式 + 恢复后删除
qa-auditor.md     → §2.3 审计报告; §3.3 Training 输出
project-manager.md → §5.5 User Feedback; §6.1 Req List; §6.3 Risk
chief-engineer.md  → §6 Engineering Status
```

---

*Version: 2.0.0 — 项目级数据管理 SOP (目录+命名+写入删除+索引+切片)*
*Created: 2026-02-15*


### 6.4 去重与加权（强制）

- ERROR-BOOK 与 PROJECT-MEMORY 均执行“命中即加权、禁止重复录入”。
- 命中规则：关键词 + 指纹（模块/场景/后果）
- 命中后仅更新 `count/weight/last_seen`，不新增重复条目。
